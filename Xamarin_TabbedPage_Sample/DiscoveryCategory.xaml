<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_TabbedPage_Sample.DiscoveryCategory"
             x:Name="this">
    <ContentView.Content>
        <Frame x:Name="Category"
               HasShadow="False"
               BackgroundColor="White"
               Margin="10,5"
               BindingContext="{x:Reference this}">
            <Frame.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnCategoryToggle"/>
            </Frame.GestureRecognizers>
            <StackLayout
                Margin="10">
                <Image Source="{Binding CategoryImageSource}" HorizontalOptions="CenterAndExpand"/>
                <Label Text="{Binding CategoryTitle}" Style="{StaticResource titleStyle}"
                       Font="20,Bold"
                       HorizontalTextAlignment="Center"/>
                <Label x:Name="Availability"
                       Text="{Binding CategoryAvailability}" Style="{StaticResource subtittleStyle}"
                       HorizontalTextAlignment="Center"
                       Font="18,Bold"/> 
            </StackLayout>
        </Frame>
    </ContentView.Content>
</ContentView>